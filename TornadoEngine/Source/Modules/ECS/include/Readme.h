#pragma once

/*
  System and Feature:

  Create and use systems (TInitSystem, TExecuteSystem, TReactiveSystem ( that use entt::registry::view ) )
  only in HEADERs. Don't use it in cpp, cc files.
*/
